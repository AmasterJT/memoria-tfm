\crearanexo{Manual de despliegue}{anexo:manual_de_despliegue}

Este anexo describe el procedimiento necesario para desplegar el laboratorio desarrollado en el presente trabajo. Se presentan dos métodos alternativos de instalación: el uso de máquinas virtuales preconfiguradas y la instalación manual desde cero.

% \subsection*{Requisitos previos}

% Para la ejecución del laboratorio se recomienda disponer de un equipo anfitrión con las siguientes características mínimas:

% \begin{itemize}
%     \item Procesador de cuatro núcleos o superior.
%     \item 16 GB de memoria RAM.
%     \item Al menos 150 GB de espacio libre en disco.
%     \item VMware Workstation Pro 17 o superior.
%     \item Acceso a Internet para la descarga de dependencias.
% \end{itemize}

Como primer paso debemos configurar la red VMnet10 en VMware Workstation, que será utilizada para la comunicación entre los sistemas corporativos del laboratorio. Esta red se configura como una red host-only sin DHCP, con el bloque de direcciones IP

\subsection*{Configuración de la red VMnet10 en VMware Workstation}

Configuración de dos redes virtuales en VMware Workstation. N Normalmente \texttt{VMnet8} viene preconfigurada como red NAT, por lo que es posible que ya exista, sin embargo, es necesario crear una nueva red interna  \texttt{VMnet10} para la simulación de la red corporativa.

\vspace{0.5cm}
\begin{tcolorbox}[
    enhanced,
    breakable,
    colback=black!3,
    colframe=black!15,
    boxrule=0.2pt,
    arc=2mm,
    left=4mm,
    right=4mm,
    top=2mm,
    bottom=2mm
]

En VMware Workstation.
\begin{enumerate}

    \item Acceder al menú superior: \texttt{Edit} $\rightarrow$ \texttt{Virtual Network Editor}

    \item Pulsar sobre \texttt{Change Settings} para habilitar permisos de administrador.

    \item Seleccionar una red libre, por ejemplo \texttt{VMnet10}. En caso de que no exista, crearla mediante la opción \texttt{Add Network}.
    
    \item Seleccionamos en el desplegable \texttt{VMnet10}

    \item Configurar \texttt{VMnet10} como red interna seleccionando la opción:
    
    
    \begin{itemize}
        \item Host-only.
        \item Desactivar \textit{conect a host virtual adapter to his network}.
        \item Desactivar el servidor DHCP.
    \end{itemize}

    \begin{itemize}
        \item \texttt{Subnet IP: 10.10.10.0}.
        \item \texttt{Subnet mask: 255.255.255.0}.
    \end{itemize}


    \item Aplicar los cambios pulsando \texttt{Apply} y después \texttt{OK}.

\end{enumerate}

\end{tcolorbox}


\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Imagenes/proceso-configuracion-red-vmware.png}

    \vspace{0.2cm}
    \captionazul{Configuración de la red interna VMnet10 en VMware Workstation.}

    \label{fig:configuracion_red_vmware}
\end{figure}




Una vez llegamos a este punto, la red interna VMnet10 queda correctamente configurada y lista para ser utilizada por las máquinas virtuales del laboratorio. Ahora falta asignarlas a cada máquina virtual según sea el caso.

Dependiendo del método de despliegue elegido, las máquinas virtuales pueden venir preconfiguradas con la interfaz de red interna VMnet10 o bien será necesario asignarla manualmente durante la instalación.

\subsection*{Métodos de despliegue}

Con el objetivo de facilitar la reproducibilidad del laboratorio, se han definido dos alternativas de instalación:

\begin{enumerate}
    \item Instalación mediante máquinas virtuales preconfiguradas.
    \item Instalación manual desde cero.
\end{enumerate}

% ======================================================
\subsection*{Instalación mediante máquinas virtuales preconfiguradas}
% ======================================================

Este método permite disponer del laboratorio completamente funcional en pocos minutos. Las máquinas virtuales ya contienen todas las configuraciones y vulnerabilidades implementadas durante el desarrollo del proyecto.

\subsubsection*{Descarga de las máquinas virtuales}

En primer lugar, deben descargarse las máquinas virtuales correspondientes a:

\begin{itemize}
    \item Ubuntu Server 22.04.
    \item DC01 (Windows Server 2012).
    \item DEV01 (Windows 7).
\end{itemize}

\subsubsection*{Importación en VMware Workstation}

Una vez descargados los archivos, se procede a:

\begin{enumerate}
    \item Descomprimir el contenido.
    \item Importar las tres máquinas virtuales en VMware Workstation.
    \item Verificar la configuración de las interfaces de red.
    \item Encender las máquinas virtuales.
\end{enumerate}








% ======================================================
\clearpage
\subsection*{Instalación manual}
% ======================================================

Alternativamente, la infraestructura puede desplegarse desde cero mediante la instalación manual de cada una de las máquinas virtuales. Este procedimiento permite comprender con mayor detalle la configuración interna del laboratorio y facilita su adaptación a otros entornos.

\subsubsection*{Creación de las máquinas virtuales}

Antes de instalar los sistemas operativos, se deben crear las máquinas virtuales en VMware Workstation utilizando la configuración recomendada en la Figura~\ref{fig:configuracion_vm} o, alternativamente, los valores optimizados recogidos en la Tabla~\ref{tab:optimizacion_memoria}. Además, cada máquina debe disponer de los adaptadores de red necesarios:


\begin{itemize}
    \item \textbf{Ubuntu Server:} adaptador NAT mediante VMnet8 y adaptador interno mediante VMnet10.
    \item \textbf{DC01:} adaptador interno mediante VMnet10.
    \item \textbf{DEV01:} adaptador interno mediante VMnet10.
\end{itemize}

% ------------------------------------------------------
\subsubsection*{Instalación del servidor Ubuntu}
% ------------------------------------------------------

El servidor Ubuntu actúa como máquina víctima principal y aloja el portal web vulnerable. Para su instalación se utiliza Ubuntu Server 22.04 con la siguiente configuración:

\begin{itemize}
    \item 2 vCPU.
    \item 3 GB de memoria RAM.
    \item 40 GB de disco.
    \item Adaptador 1: VMnet8 (NAT).
    \item Adaptador 2: VMnet10 (red interna).
\end{itemize}

\subsubsubsection*{Configuración de red en Ubuntu}

Una vez instalado el sistema operativo, se configura la interfaz interna con una dirección IP estática dentro de la red \texttt{10.10.10.10/24}.

Para ello se edita el fichero de configuración de Netplan:

\begin{bashbox}
sudo nano /etc/netplan/00-installer-config.yaml
\end{bashbox}

El contenido del fichero debe ser similar al siguiente. Los nombres de las interfaces pueden variar según la configuración de VMware, por lo que deben comprobarse previamente con el comando \texttt{ip a}.

\begin{configbox}
network:
    version: 2
    ethernets:
    ens33:
        dhcp4: true
    ens34:
        dhcp4: false
        addresses:
        - 10.10.10.10/24
\end{configbox}

Después de guardar los cambios, se aplica la configuración:

\begin{bashbox}
sudo netplan apply
\end{bashbox}

Finalmente, se comprueba que la dirección IP se ha asignado correctamente:

\begin{bashbox}
ip a
\end{bashbox}

\subsubsubsection*{Descarga del laboratorio}

Una vez configurada la red, se clona el repositorio del proyecto:

\begin{bashbox}
git clone https://github.com/AmasterJT/TFM-Ciber-CEU-Jose-Torre.git

cd TFM-Ciber-CEU-Jose-Torre
\end{bashbox}

\subsubsubsection*{Ejecución del script de despliegue}

A continuación se conceden permisos de ejecución al script de instalación y se ejecuta con privilegios de administrador:

\begin{bashbox}
chmod +x scripts/setup_tfm_lab.sh
sudo ./scripts/setup_tfm_lab.sh
\end{bashbox}

Este script realiza automáticamente las siguientes acciones:

\begin{itemize}
    \item Instalación de Docker y Docker Compose Plugin.
    \item Configuración de usuarios y grupos.
    \item Creación de credenciales simuladas.
    \item Habilitación del acceso SSH.
    \item Preparación del escenario de escalada de privilegios.
    \item Despliegue del portal vulnerable mediante Docker Compose.
\end{itemize}

% ------------------------------------------------------
\subsubsection*{Instalación del controlador de dominio DC01}
% ------------------------------------------------------

La máquina \texttt{DC01} actúa como controlador de dominio principal del laboratorio. Se instala utilizando Windows Server 2012 en español con la siguiente configuración:

\begin{itemize}
    \item 2 vCPU.
    \item 4 GB de memoria RAM.
    \item 60 GB de disco.
    \item Adaptador de red conectado a VMnet10.
\end{itemize}

\subsubsubsection*{Configuración de la dirección IP en DC01}

Una vez instalado Windows Server 2012, se configura una dirección IP estática desde la interfaz gráfica del sistema.

El procedimiento es el siguiente:

\begin{tcolorbox}[
    enhanced,
    breakable,
    colback=black!3,
    colframe=black!15,
    boxrule=0.2pt,
    arc=2mm,
    left=4mm,
    right=4mm,
    top=2mm,
    bottom=2mm
]
\begin{enumerate}
    \item Abrir el \textbf{Panel de control}.
    \item Acceder a \textbf{Redes e Internet}.
    \item Entrar en \textbf{Centro de redes y recursos compartidos}.
    \item Seleccionar \textbf{Cambiar configuración del adaptador}.
    \item Hacer clic derecho sobre el adaptador de red y seleccionar \textbf{Propiedades}.
    \item Seleccionar \textbf{Protocolo de Internet versión 4 (TCP/IPv4)}.
    \item Pulsar en \textbf{Propiedades}.
    \item Configurar los valores indicados a continuación.
\end{enumerate}
\end{tcolorbox}


\renewcommand{\arraystretch}{1.5}

\begin{table}[h]
    \centering
    \captionazul{Configuración de la dirección IP en DC01.}

    \setlength{\arrayrulewidth}{0.05mm}

    \begin{tabular}{|l|c|}
        \hline
        \rowcolor{blue!10}
        \textbf{Parámetro} & \textbf{Valor} \\
        \hline
        Dirección IP & 10.10.10.5 \\
        \hline
        Máscara de subred & 255.255.255.0 \\
        \hline
        Puerta de enlace & Vacía \\
        \hline
        Servidor DNS preferido & 10.10.10.10 \\
        \hline
    \end{tabular}

    \renewcommand{\arraystretch}{1}
    \label{tab:stack_tecnologico_dc01}
\end{table}



\subsubsubsection*{Instalación de Active Directory}

Una vez configurada la red, se instalan los roles necesarios para convertir el servidor en controlador de dominio.

\begin{tcolorbox}[
    enhanced,
    breakable,
    colback=black!3,
    colframe=black!15,
    boxrule=0.2pt,
    arc=2mm,
    left=4mm,
    right=4mm,
    top=2mm,
    bottom=2mm
]
\begin{enumerate}
    \item Abrir el \textbf{Administrador del servidor}.
    \item Seleccionar \textbf{Administrar}.
    \item Pulsar en \textbf{Agregar roles y características}.
    \item Seleccionar \textbf{Instalación basada en características o en roles}.
    \item Elegir el servidor \texttt{DC01}.
    \item Marcar el rol \textbf{Servicios de dominio de Active Directory}.
    \item Aceptar la instalación de las características adicionales requeridas.
    \item Marcar también el rol \textbf{Servidor DNS}, si no se selecciona automáticamente.
    \item Continuar el asistente y pulsar \textbf{Instalar}.
\end{enumerate}
\end{tcolorbox}

\subsubsubsection*{Promoción a controlador de dominio}

Tras instalar el rol de Active Directory, el servidor debe promocionarse a controlador de dominio.

\begin{tcolorbox}[
    enhanced,
    breakable,
    colback=black!3,
    colframe=black!15,
    boxrule=0.2pt,
    arc=2mm,
    left=4mm,
    right=4mm,
    top=2mm,
    bottom=2mm
]
\begin{enumerate}
    \item En el \textbf{Administrador del servidor}, pulsar sobre la notificación superior.
    \item Seleccionar \textbf{Promover este servidor a controlador de dominio}.
    \item Elegir la opción \textbf{Agregar un nuevo bosque}.
    \item Introducir como nombre de dominio raíz: \texttt{corp.local}.
    \item Configurar la contraseña de restauración de servicios de directorio.
    \item Mantener las opciones por defecto para DNS y NetBIOS.
    \item Revisar la configuración.
    \item Pulsar \textbf{Instalar}.
    \item Esperar a que finalice el proceso y permitir el reinicio del servidor.
\end{enumerate}
\end{tcolorbox}

Tras el reinicio, el servidor \texttt{DC01} quedará configurado como controlador de dominio del dominio \texttt{corp.local}.

\subsubsubsection*{Configuración básica del dominio}

Una vez creado el dominio, se configuran los elementos básicos necesarios para el laboratorio:

\begin{itemize}
    \item Usuarios corporativos simulados.
    \item Grupos de seguridad.
    \item Recursos compartidos SMB.
    \item Políticas básicas de dominio.
    \item Servicios de administración remota.
\end{itemize}

% ------------------------------------------------------
\subsubsection*{Instalación de la estación DEV01}
% ------------------------------------------------------

La máquina \texttt{DEV01} representa una estación de trabajo corporativa integrada en el dominio. Se instala utilizando Windows 7 Professional en español con la siguiente configuración:

\begin{itemize}
    \item 2 vCPU.
    \item 3 GB de memoria RAM.
    \item 40 GB de disco.
    \item Adaptador de red conectado a VMnet10.
\end{itemize}

\subsubsubsection*{Configuración de la dirección IP en DEV01}

Tras instalar Windows 7, se configura la dirección IP estática del equipo desde la interfaz gráfica.

\begin{tcolorbox}[
    enhanced,
    breakable,
    colback=black!3,
    colframe=black!15,
    boxrule=0.2pt,
    arc=2mm,
    left=4mm,
    right=4mm,
    top=2mm,
    bottom=2mm
]
\begin{enumerate}
    \item Abrir el \textbf{Panel de control}.
    \item Entrar en \textbf{Redes e Internet}.
    \item Acceder al \textbf{Centro de redes y recursos compartidos}.
    \item Seleccionar \textbf{Cambiar configuración del adaptador}.
    \item Hacer clic derecho sobre el adaptador de red y seleccionar \textbf{Propiedades}.
    \item Seleccionar \textbf{Protocolo de Internet versión 4 (TCP/IPv4)}.
    \item Pulsar en \textbf{Propiedades}.
    \item Introducir la configuración indicada a continuación.
\end{enumerate}
\end{tcolorbox}



\renewcommand{\arraystretch}{1.5}

\begin{table}[h]
    \centering
    \captionazul{Configuración de la dirección IP en DEV01.}

    \setlength{\arrayrulewidth}{0.05mm}

    \begin{tabular}{|l|c|}
        \hline
        \rowcolor{blue!10}
        \textbf{Parámetro} & \textbf{Valor} \\
        \hline
        Dirección IP & 10.10.10.20 \\
        \hline
        Máscara de subred & 255.255.255.0 \\
        \hline
        Puerta de enlace & Vacía \\
        \hline
        Servidor DNS preferido & 10.10.10.10 \\
        \hline
    \end{tabular}

    \renewcommand{\arraystretch}{1}
    \label{tab:stack_tecnologico_dev01}
\end{table}



\subsubsubsection*{Unión de DEV01 al dominio}

Una vez configurada la IP y comprobada la conectividad con \texttt{DC01}, la máquina se une al dominio \texttt{corp.local}.

\begin{tcolorbox}[
    enhanced,
    breakable,
    colback=black!3,
    colframe=black!15,
    boxrule=0.2pt,
    arc=2mm,
    left=4mm,
    right=4mm,
    top=2mm,
    bottom=2mm
]
\begin{enumerate}
    \item Abrir el \textbf{Panel de control}.
    \item Entrar en \textbf{Sistema y seguridad}.
    \item Acceder a \textbf{Sistema}.
    \item Pulsar en \textbf{Cambiar configuración}.
    \item En la pestaña \textbf{Nombre de equipo}, pulsar \textbf{Cambiar}.
    \item Seleccionar la opción \textbf{Dominio}.
    \item Introducir el dominio \texttt{corp.local}.
    \item Autenticarse con una cuenta con permisos para unir equipos al dominio.
    \item Aceptar el mensaje de bienvenida al dominio.
    \item Reiniciar la máquina.
\end{enumerate}
\end{tcolorbox}

Tras el reinicio, \texttt{DEV01} quedará integrada en el dominio y podrá autenticarse con usuarios corporativos.

% ======================================================
\subsubsection*{Puesta en marcha}
% ======================================================

Una vez desplegada la infraestructura, se recomienda iniciar las máquinas en el siguiente orden:

\begin{enumerate}
    \item DC01.
    \item DEV01.
    \item Ubuntu Server.
    \item Máquina atacante.
\end{enumerate}

Este orden permite garantizar que los servicios de dominio y resolución DNS estén disponibles antes de iniciar el resto de sistemas.

\subsubsection*{Comprobaciones básicas}

Antes de comenzar los ejercicios de explotación, se recomienda realizar las siguientes comprobaciones.

\subsubsubsection*{Comprobaciones desde Ubuntu}

\begin{bashbox}
ping 10.10.10.10
ping 10.10.10.30
\end{bashbox}

\subsubsubsection*{Comprobaciones desde DEV01}

Desde una consola de Windows se puede comprobar la conectividad con el controlador de dominio:

\begin{configbox}
ping DC01
ping corp.local
\end{configbox}

\subsubsubsection*{Comprobación del portal web}

Desde la máquina atacante o desde un equipo con conectividad hacia el servidor Ubuntu, se comprueba el acceso al portal vulnerable:

\begin{configbox}
http://10.10.10.10:8080/portal_pyme
\end{configbox}

Tras completar estas comprobaciones, el laboratorio quedará preparado para ejecutar los escenarios de explotación descritos en el Capítulo 6.